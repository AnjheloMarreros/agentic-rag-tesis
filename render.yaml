services:
  - type: web
    name: argumentation-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m backend.scripts.build_knowledge_base
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true